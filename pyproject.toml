[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "airflow-provider-aiida"
authors = [
    { name = "AiiDA Team", email = "developers@aiida.net" },
]
description = "The Apache Airflow provider for AiiDA"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: Apache Airflow",
    "Framework :: Apache Airflow :: Provider",
    "Programming Language :: Python :: 3.11",
]
dynamic = [
    "version"
]
requires-python = ">=3.11.0,<3.12.0"
dependencies = [
    "apache-airflow>=3.1.0",
    "aiida-core @ git+https://github.com/aiidateam/aiida-core@airflow-dev",
    "click>=8.0"
]

[project.optional-dependencies]
tests = [
    "pytest>=7.0",
    "pytest-mock>=3.10",
]

[project.urls]
Homepage = "..." # TODO
Source = "..." # TODO

[project.readme]
file = "README.md"
content-type = "text/markdown"

[project.entry-points.apache_airflow_provider]
provider_info = "airflow_provider_aiida.__init__:get_provider_info"

[project.entry-points."aiida.dags"]
aiida-standard = "airflow_provider_aiida.example_dags"

[project.entry-points."aiida.workflows"]
"airflow_provider_aiida.restart" = "airflow_provider_aiida.workflows.restart:ArithmeticRestartWorkChain"

[project.scripts]
airdi = "airflow_provider_aiida.cli:airdi"
airflow-provider-aiida-triggerer-service = "airflow_provider_aiida.aiida_core.engine.daemon.triggerer_service:main"

[tool.hatch.version]
path = "src/airflow_provider_aiida/__init__.py"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.envs.default]
python = "3.11"

[tool.hatch.envs.hatch-test]
default-args = []
installer = "uv"
dependencies = [
    "pytest>=7.0",
    "pytest-mock>=3.10",
    # NOTE: these are requirements only needed with psql
    "psycopg2-binary>=2.9.0",
    "asyncpg>=0.29.0",
]

[[tool.hatch.envs.hatch-test.matrix]]
python = ["3.11"]

[tool.hatch.envs.hatch-test.env-vars]
# PostgreSQL configuration (shared by Airflow and AiiDA)
POSTGRES_HOST = "127.0.0.1"
POSTGRES_PORT = "5434"
POSTGRES_USER = "postgres"
POSTGRES_PASSWORD = "postgres"


# AiiDA configuration
AIIDA_PATH = "{root}/.pytest"
AIRFLOW_PROVIDER_AIIDA__TESTS__AIIDA_PROFILE = "test"

[tool.hatch.envs.hatch-test.scripts]
# Setup test environment
setup-profile = [
  "airdi presto --postgres-hostname {env:POSTGRES_HOST} --postgres-port {env:POSTGRES_PORT} --postgres-username {env:POSTGRES_USER} --postgres-password {env:POSTGRES_PASSWORD} --profile-name {env:AIRFLOW_PROVIDER_AIIDA__TESTS__AIIDA_PROFILE}",
]

# TODO this needs to be replaced
teardown-profile = [
  "python scripts/cmd_profile_delete.py --profile-name {env:AIRFLOW_PROVIDER_AIIDA__TESTS__AIIDA_PROFILE} --postgres-user {env:POSTGRES_USER} --postgres-password {env:POSTGRES_PASSWORD}"
]

# Process manager commands
start-psql-service = "docker compose -f docker-compose.test.yml up -d"
stop-psql-service = "docker compose -f docker-compose.test.yml down -v"
status-psql-service = "docker ps"

# Services commands
services-start = "airdi services start"
services-stop = "airdi services stop"
services-status = "airdi services status"

# Start individual Airflow services (for manual testing)
scheduler = "airdi airflow scheduler"
api-server = "airdi airflow api-server"
triggerer = "airdi airflow triggerer"
dag-processor = "airdi airflow dag-processor"

# Run integration tests
unit-tests = "pytest -m 'not integration' {args:{root}/tests}"
integration-tests = "pytest -m 'integration' {args:{root}/tests}"
# Default test runner - excludes integration tests, only runs tests/ directory
run = "pytest {args:{root}/tests}"

kill-zombies = [
   "pkill -f 'airflow dag-processor'",
   "pkill -f 'airflow api-server'",
   "pkill -f 'airflow scheduler'",
   "pkill -f 'airflow triggerer'",
]

# Clean test artifacts
# NOTE: Only use after services have been stopped to
clean = [
    "rm -rf .pytest",
    "docker volume rm -f airflow-provider-aiida_postgres-db-volume",
    "echo 'Cleaned Docker volumes and .pytest directory'",
]

[tool.setuptools]
include-package-data = false

[tool.setuptools.dynamic]
version = { attr = "aiida_provider.__version__" }

[tool.setuptools.packages.find]
exclude = [
    "*tests.*",
    "*tests"
]

[tool.pytest.ini_options]
markers = [
    "integration: marks tests that require Airflow services to be running (scheduler, triggerer)",
]
filterwarnings = [
    # AiiDA SQLAlchemy warnings - these are expected and safe to ignore as done in aiida-core
    "ignore:Object of type .* not in session, .* operation along .* will not proceed:sqlalchemy.exc.SAWarning",
    "ignore:Identity map already had an identity for .* inside of an event handler within the flush?:sqlalchemy.exc.SAWarning",
    # AiiDA configuration warnings in test environments
    "ignore:Creating AiiDA configuration folder.*:UserWarning",
    # Airflow deprecation warnings
    "ignore:'HTTP_422_UNPROCESSABLE_ENTITY' is deprecated.*:DeprecationWarning",
]
